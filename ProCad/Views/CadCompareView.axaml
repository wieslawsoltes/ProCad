<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:ProCad.ViewModels"
             mc:Ignorable="d"
             x:Class="ProCad.Views.CadCompareView"
             x:DataType="vm:CadCompareViewModel">
  <Grid RowDefinitions="Auto,Auto,Auto,*"
        ColumnDefinitions="*,*"
        RowSpacing="12"
        ColumnSpacing="12"
        Margin="16">
    <Grid Grid.ColumnSpan="2"
          ColumnDefinitions="*,Auto">
      <TextBlock Text="Compare Documents"
                 FontSize="20"
                 FontWeight="SemiBold" />
      <Button Grid.Column="1"
              Classes="fluent-icon" Tag="{StaticResource SyncIconPath}" Content="Refresh"
              Command="{CompiledBinding RefreshCommand}" />
    </Grid>

    <TextBlock Grid.Row="1"
               Grid.ColumnSpan="2"
               Text="{CompiledBinding StatusMessage}"
               TextWrapping="Wrap"
               Foreground="{DynamicResource ThemeForegroundLowBrush}" />

    <Border Grid.Row="2"
            BorderThickness="1"
            CornerRadius="8"
            BorderBrush="{DynamicResource ThemeBorderLowBrush}"
            Padding="12">
      <StackPanel Spacing="6">
        <TextBlock Text="{CompiledBinding Left.Label}"
                   FontWeight="SemiBold" />
        <StackPanel Orientation="Horizontal"
                    Spacing="6">
          <Button Classes="fluent-icon" Tag="{StaticResource OpenFolderIconPath}" Content="Load"
                  Command="{CompiledBinding LoadLeftCommand}" />
          <Button Classes="fluent-icon" Tag="{StaticResource CheckmarkCircleIconPath}" Content="Use Active"
                  Command="{CompiledBinding UseActiveAsLeftCommand}" />
        </StackPanel>
        <TextBlock Text="{CompiledBinding Left.DisplayName}"
                   FontWeight="SemiBold" />
        <TextBlock Text="{CompiledBinding Left.FormatText}" />
        <TextBlock Text="{CompiledBinding Left.PathText}"
                   TextTrimming="CharacterEllipsis" />
      </StackPanel>
    </Border>

    <Border Grid.Row="2"
            Grid.Column="1"
            BorderThickness="1"
            CornerRadius="8"
            BorderBrush="{DynamicResource ThemeBorderLowBrush}"
            Padding="12">
      <StackPanel Spacing="6">
        <TextBlock Text="{CompiledBinding Right.Label}"
                   FontWeight="SemiBold" />
        <StackPanel Orientation="Horizontal"
                    Spacing="6">
          <Button Classes="fluent-icon" Tag="{StaticResource OpenFolderIconPath}" Content="Load"
                  Command="{CompiledBinding LoadRightCommand}" />
          <Button Classes="fluent-icon" Tag="{StaticResource CheckmarkCircleIconPath}" Content="Use Active"
                  Command="{CompiledBinding UseActiveAsRightCommand}" />
        </StackPanel>
        <TextBlock Text="{CompiledBinding Right.DisplayName}"
                   FontWeight="SemiBold" />
        <TextBlock Text="{CompiledBinding Right.FormatText}" />
        <TextBlock Text="{CompiledBinding Right.PathText}"
                   TextTrimming="CharacterEllipsis" />
      </StackPanel>
    </Border>

    <Grid Grid.Row="3"
          Grid.ColumnSpan="2"
          RowDefinitions="Auto,Auto,Auto,*"
          ColumnDefinitions="*,*"
          RowSpacing="8"
          ColumnSpacing="12">
      <TextBlock Grid.ColumnSpan="2"
                 Text="Diff Summary"
                 FontWeight="SemiBold" />
      <StackPanel Grid.Row="1"
                  Grid.ColumnSpan="2"
                  Orientation="Horizontal"
                  Spacing="8">
        <TextBlock Text="Search"
                   VerticalAlignment="Center" />
        <TextBox Width="200"
                 Watermark="Search..."
                 Text="{CompiledBinding SummarySearchText, UpdateSourceTrigger=PropertyChanged}" />
        <Button Classes="fluent-icon" Tag="{StaticResource ClearIconPath}" Content="Clear"
                Command="{CompiledBinding ClearSummarySearchCommand}" />
        <TextBlock Text="Filter"
                   Margin="12,0,0,0"
                   VerticalAlignment="Center" />
        <TextBox Width="200"
                 Watermark="Filter..."
                 Text="{CompiledBinding SummaryFilterText, UpdateSourceTrigger=PropertyChanged}" />
        <Button Classes="fluent-icon" Tag="{StaticResource ClearIconPath}" Content="Clear"
                Command="{CompiledBinding ClearSummaryFilterCommand}" />
      </StackPanel>
      <DataGrid Grid.Row="2"
                Grid.ColumnSpan="2"
                ItemsSource="{CompiledBinding SummaryView}"
                ColumnDefinitionsSource="{CompiledBinding SummaryColumnDefinitions}"
                SortingModel="{CompiledBinding SummarySortingModel}"
                FilteringModel="{CompiledBinding SummaryFilteringModel}"
                SearchModel="{CompiledBinding SummarySearchModel}"
                AutoGenerateColumns="False"
                CanUserSortColumns="True"
                CanUserReorderColumns="False"
                CanUserResizeColumns="True"
                GridLinesVisibility="All"
                IsReadOnly="True" />

      <TabControl Grid.Row="3"
                  Grid.ColumnSpan="2">
        <TabItem Header="Object Model">
          <Grid RowDefinitions="Auto,Auto,*"
                ColumnDefinitions="*,*"
                RowSpacing="8"
                ColumnSpacing="12">
            <TextBlock Text="Object Changes"
                       FontWeight="SemiBold" />
            <TextBlock Grid.Column="1"
                       Text="{CompiledBinding SelectedDiffTitle}"
                       FontWeight="SemiBold"
                       TextTrimming="CharacterEllipsis" />

            <StackPanel Grid.Row="1"
                        Orientation="Horizontal"
                        Spacing="8">
              <TextBox Width="180"
                       Watermark="Search..."
                       Text="{CompiledBinding ObjectSearchText, UpdateSourceTrigger=PropertyChanged}" />
              <Button Classes="fluent-icon" Tag="{StaticResource ClearIconPath}" Content="Clear"
                      Command="{CompiledBinding ClearObjectSearchCommand}" />
              <TextBox Width="180"
                       Watermark="Filter..."
                       Text="{CompiledBinding ObjectFilterText, UpdateSourceTrigger=PropertyChanged}" />
              <Button Classes="fluent-icon" Tag="{StaticResource ClearIconPath}" Content="Clear"
                      Command="{CompiledBinding ClearObjectFilterCommand}" />
            </StackPanel>
            <StackPanel Grid.Row="1"
                        Grid.Column="1"
                        Orientation="Horizontal"
                        Spacing="8">
              <TextBox Width="180"
                       Watermark="Search..."
                       Text="{CompiledBinding PropertySearchText, UpdateSourceTrigger=PropertyChanged}" />
              <Button Classes="fluent-icon" Tag="{StaticResource ClearIconPath}" Content="Clear"
                      Command="{CompiledBinding ClearPropertySearchCommand}" />
              <TextBox Width="180"
                       Watermark="Filter..."
                       Text="{CompiledBinding PropertyFilterText, UpdateSourceTrigger=PropertyChanged}" />
              <Button Classes="fluent-icon" Tag="{StaticResource ClearIconPath}" Content="Clear"
                      Command="{CompiledBinding ClearPropertyFilterCommand}" />
            </StackPanel>

            <DataGrid Grid.Row="2"
                      ItemsSource="{CompiledBinding ObjectDiffsView}"
                      ColumnDefinitionsSource="{CompiledBinding ObjectDiffColumnDefinitions}"
                      SortingModel="{CompiledBinding ObjectSortingModel}"
                      FilteringModel="{CompiledBinding ObjectFilteringModel}"
                      SearchModel="{CompiledBinding ObjectSearchModel}"
                      SelectedItem="{CompiledBinding SelectedObjectDiff}"
                      AutoGenerateColumns="False"
                      CanUserSortColumns="True"
                      CanUserReorderColumns="False"
                      CanUserResizeColumns="True"
                      GridLinesVisibility="All"
                      IsReadOnly="True" />

            <DataGrid Grid.Row="2"
                      Grid.Column="1"
                      ItemsSource="{CompiledBinding PropertyDiffsView}"
                      ColumnDefinitionsSource="{CompiledBinding PropertyDiffColumnDefinitions}"
                      SortingModel="{CompiledBinding PropertySortingModel}"
                      FilteringModel="{CompiledBinding PropertyFilteringModel}"
                      SearchModel="{CompiledBinding PropertySearchModel}"
                      AutoGenerateColumns="False"
                      CanUserSortColumns="True"
                      CanUserReorderColumns="False"
                      CanUserResizeColumns="True"
                      GridLinesVisibility="All"
                      IsReadOnly="True" />
          </Grid>
        </TabItem>
        <TabItem Header="DXF Text">
          <Grid RowDefinitions="Auto,Auto,*"
                RowSpacing="8">
            <TextBlock Text="{CompiledBinding DxfTextStatusMessage}"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource ThemeForegroundLowBrush}" />
            <StackPanel Grid.Row="1"
                        Orientation="Horizontal"
                        Spacing="8">
              <TextBox Width="220"
                       Watermark="Search..."
                       Text="{CompiledBinding DxfTextSearchText, UpdateSourceTrigger=PropertyChanged}" />
              <Button Classes="fluent-icon" Tag="{StaticResource ClearIconPath}" Content="Clear"
                      Command="{CompiledBinding ClearDxfTextSearchCommand}" />
              <TextBox Width="220"
                       Watermark="Filter..."
                       Text="{CompiledBinding DxfTextFilterText, UpdateSourceTrigger=PropertyChanged}" />
              <Button Classes="fluent-icon" Tag="{StaticResource ClearIconPath}" Content="Clear"
                      Command="{CompiledBinding ClearDxfTextFilterCommand}" />
            </StackPanel>
            <DataGrid Grid.Row="2"
                      ItemsSource="{CompiledBinding DxfTextDiffsView}"
                      ColumnDefinitionsSource="{CompiledBinding DxfTextColumnDefinitions}"
                      SortingModel="{CompiledBinding DxfTextSortingModel}"
                      FilteringModel="{CompiledBinding DxfTextFilteringModel}"
                      SearchModel="{CompiledBinding DxfTextSearchModel}"
                      AutoGenerateColumns="False"
                      CanUserSortColumns="True"
                      CanUserReorderColumns="False"
                      CanUserResizeColumns="True"
                      GridLinesVisibility="All"
                      IsReadOnly="True" />
          </Grid>
        </TabItem>
      </TabControl>
    </Grid>
  </Grid>
</UserControl>
