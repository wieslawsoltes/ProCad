<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:ProCad.ViewModels"
             xmlns:converters="using:ProCad.Converters"
             mc:Ignorable="d"
             x:Class="ProCad.Views.CadEditorToolPanelView"
             x:DataType="vm:CadEditorToolPanelViewModel">
  <UserControl.Styles>
    <Style Selector="ItemsControl.editor-tool-list > ContentPresenter">
      <Setter Property="HorizontalAlignment" Value="Stretch" />
    </Style>
  </UserControl.Styles>
  <UserControl.Resources>
    <converters:IconResourceKeyToGeometryConverter x:Key="IconResourceKeyToGeometryConverter" />
  </UserControl.Resources>
  <ScrollViewer Margin="10"
                VerticalScrollBarVisibility="Auto"
                HorizontalScrollBarVisibility="Disabled">
    <StackPanel Spacing="8">
      <Border Padding="8"
              CornerRadius="8"
              BorderBrush="{DynamicResource ThemeBorderLowBrush}"
              BorderThickness="1">
        <Grid RowDefinitions="Auto,Auto,Auto,Auto"
              RowSpacing="6">
          <DockPanel LastChildFill="False">
            <TextBlock Text="{CompiledBinding ActiveCommand, StringFormat='Active: {0}'}"
                       FontWeight="SemiBold" />
            <Button DockPanel.Dock="Right"
                    Classes="fluent-icon" Tag="{StaticResource DismissCircleIconPath}" Content="Esc"
                    Command="{CompiledBinding CancelCommand}"
                    IsVisible="{CompiledBinding IsCommandActive}" />
          </DockPanel>
          <TextBlock Grid.Row="1"
                     Text="{CompiledBinding ParameterHelp}"
                     TextWrapping="Wrap"
                     FontSize="11"
                     Opacity="0.88"
                     IsVisible="{CompiledBinding IsCommandActive}" />
          <TextBlock Grid.Row="2"
                     Text="{CompiledBinding StatusMessage}"
                     FontSize="11"
                     TextWrapping="Wrap"
                     Opacity="0.75"
                     IsVisible="{CompiledBinding HasStatusMessage}" />
          <ItemsControl Grid.Row="3"
                        ItemsSource="{CompiledBinding Completions}"
                        IsVisible="{CompiledBinding HasCompletions}">
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate>
                <WrapPanel Orientation="Horizontal" />
              </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
              <DataTemplate x:DataType="vm:CadCommandCompletionItemViewModel">
                <Button Content="{CompiledBinding DisplayText}"
                        Command="{CompiledBinding ApplyCommand}"
                        CommandParameter="{CompiledBinding .}"
                        Margin="0,0,4,4"
                        MinHeight="24"
                        Padding="6,1" />
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </Grid>
      </Border>

      <Border CornerRadius="8"
              BorderBrush="{DynamicResource ThemeBorderLowBrush}"
              BorderThickness="1"
              Padding="6">
        <Grid RowDefinitions="Auto,*"
              RowSpacing="6">
          <TextBlock Text="Draw"
                     FontWeight="SemiBold" />
          <ItemsControl Grid.Row="1"
                        Classes="editor-tool-list"
                        ItemsSource="{CompiledBinding DrawTools}">
            <ItemsControl.ItemTemplate>
              <DataTemplate x:DataType="vm:CadEditorToolActionViewModel">
                <Button Classes="fluent-icon"
                        Tag="{CompiledBinding IconResourceKey, Converter={StaticResource IconResourceKeyToGeometryConverter}}"
                        Content="{CompiledBinding DisplayName}"
                        Command="{CompiledBinding StartCommand}"
                        CommandParameter="{CompiledBinding Command}"
                        HorizontalAlignment="Stretch"
                        Margin="0,2" />
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </Grid>
      </Border>

      <Border CornerRadius="8"
              BorderBrush="{DynamicResource ThemeBorderLowBrush}"
              BorderThickness="1"
              Padding="6">
        <Grid RowDefinitions="Auto,*"
              RowSpacing="6">
          <TextBlock Text="Modify"
                     FontWeight="SemiBold" />
          <ItemsControl Grid.Row="1"
                        Classes="editor-tool-list"
                        ItemsSource="{CompiledBinding ModifyTools}">
            <ItemsControl.ItemTemplate>
              <DataTemplate x:DataType="vm:CadEditorToolActionViewModel">
                <Button Classes="fluent-icon"
                        Tag="{CompiledBinding IconResourceKey, Converter={StaticResource IconResourceKeyToGeometryConverter}}"
                        Content="{CompiledBinding DisplayName}"
                        Command="{CompiledBinding StartCommand}"
                        CommandParameter="{CompiledBinding Command}"
                        HorizontalAlignment="Stretch"
                        Margin="0,2" />
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </Grid>
      </Border>

      <Border CornerRadius="8"
              BorderBrush="{DynamicResource ThemeBorderLowBrush}"
              BorderThickness="1"
              Padding="6">
        <Grid RowDefinitions="Auto,*"
              RowSpacing="6">
          <TextBlock Text="Annotate"
                     FontWeight="SemiBold" />
          <ItemsControl Grid.Row="1"
                        Classes="editor-tool-list"
                        ItemsSource="{CompiledBinding AnnotateTools}">
            <ItemsControl.ItemTemplate>
              <DataTemplate x:DataType="vm:CadEditorToolActionViewModel">
                <Button Classes="fluent-icon"
                        Tag="{CompiledBinding IconResourceKey, Converter={StaticResource IconResourceKeyToGeometryConverter}}"
                        Content="{CompiledBinding DisplayName}"
                        Command="{CompiledBinding StartCommand}"
                        CommandParameter="{CompiledBinding Command}"
                        HorizontalAlignment="Stretch"
                        Margin="0,2" />
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </Grid>
      </Border>
    </StackPanel>
  </ScrollViewer>
</UserControl>
